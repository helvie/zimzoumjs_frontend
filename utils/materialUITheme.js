import { createTheme, ThemeProvider } from '@mui/material/styles';


const theme = createTheme({
  palette: {
    primary: {
      main: "#55ff00", // Remplacez YOUR_COLOR par votre couleur personnalisée
    },
  },
});

export { theme };
